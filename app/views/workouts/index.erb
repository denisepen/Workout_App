<h1> All Workouts</h1>

<% Workout.all.each do |wkt| %>
<% if wkt.user %>
<a href="/workouts/<%= wkt.id %>">User: <%= wkt.user.username.capitalize %>  |    Workout: <%= wkt.workout %> | <%= wkt.duration %> minutes </a>
 <br><br>
  <% end %>
  <% end %>

  <a href="/landing">Home Page</a> or
  <a href="/logout">Log Out</a>
